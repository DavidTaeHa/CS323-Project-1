%Horners Algorithm
%INPUTS: a0,a1,a2,...,an , x0
clear all
list = [] %list of coefficients of polynomial
degree = input("Input polynomial degree:\n")

fprintf("Input coefficients:\n");
n = degree + 1 %number of coefficients in polynomial
for i=1:n
    temp = input("")
    list = [list, temp]
end
x0 = input("Input x0:\n")
temp = list(n)
result
for i=n-1:-1:1
    temp = temp * x0 + list(i)
    for j=i-1:-1:1
        list(i) = list(i) * x0 + list(i+1)
    end
end